# Agent 委托授权愿景

## 1. 文档目的

本文档用于描述 Agent 委托授权项目的目标愿景、重构方向和后续设计文档生成要求。

当前 `design_doc` 目录下已有四份初版设计材料：

- `design_doc/Agent人机委托机制架构设计文档.md`
- `design_doc/Agent Gateway详细设计文档.md`
- `design_doc/MCP Gateway详细设计文档.md`
- `design_doc/策略中心详细设计文档.md`

这四份材料主要来自公司领导使用内部 GLM-5 模型进行的头脑风暴。由于模型能力有限，文档中既有值得保留的问题识别和架构雏形，也存在较多方向偏差、职责混乱、安全模型不完整、实现细节错误和不可落地的设计。

因此，本文档不是对四份材料的简单摘要，而是用于指导 ChatGPT 基于这些初版材料重新生成一组更合理、更安全、更可开发的正式设计文档。

## 2. 当前材料的定位

四份初版材料应被视为“问题域输入”和“头脑风暴草稿”，不应被视为最终开发依据。

可以参考的内容包括：

- 识别出的核心问题：Cookie 透传、Agent 权限过大、委托机制缺失、A2A 权限传递不清晰、环境维度由 Agent 自行传入导致越权风险。
- 初步服务拆分：Agent Gateway、MCP Gateway、策略中心。
- 初步能力方向：Token 生成与验证、Cookie 换取、Agent 注册、工具调用鉴权、策略查询、委托链传递、人在环确认、审计与监控。
- 部分业务流程：用户通过业务系统访问 Agent，Agent 携带 Token 调用 MCP Gateway，MCP Gateway 向策略中心请求权限决策并在必要时换取 Cookie 调用 MCP Server/API。

不应直接采纳的内容包括：

- 未经安全建模的 Token 结构和 Cookie 换取流程。
- 简单继承式的 A2A 权限传递规则。
- 只停留在 CRUD 层面的策略中心设计。
- 将高危能力放到 P2/P3 的优先级划分。
- 缺少信任边界、攻击面、失败默认策略、吊销传播和审计取证能力的安全章节。
- 为追求性能而牺牲安全边界的优化方案，例如 Token 预生成、Cookie 批量换取、Cookie 过度缓存等。

后续设计应保留正确的问题意识，但重构实现模型和安全边界。

## 3. 项目愿景

Agent 委托授权项目的愿景是：在公司已有 IDaaS、IAM、ALB、Agent 网关初始版本、MCP 网关初始版本的基础上，建设一套面向云侧 Agent 的安全委托授权体系，使 Agent 能够在用户明确授权、策略可控、权限最小化、操作可追溯的前提下调用工具、访问传统后端能力，并支持必要的 A2A 协作。

最终系统应实现以下目标：

- 用户不再把完整 Cookie 或全量权限直接交给 Agent。
- Agent 只能在当前任务、当前用户、当前策略、当前数据范围内执行被授权的动作。
- MCP 工具调用必须经过统一权限决策，不允许 Agent 绕过 MCP Gateway 或策略中心直接访问后端能力。
- A2A 调用必须传递可验证、可约束、可审计的委托链，而不是简单权限继承。
- 高风险操作必须支持人在环确认，并且确认结果必须绑定具体操作，不能被重放或挪用。
- 所有关键决策和调用都应具备审计、追踪和事后取证能力。

## 4. 公司背景与系统边界

公司内已经存在以下基础设施：

- IDaaS：SSO 单点登录服务，负责用户登录和用户身份认证。
- IAM：鉴权服务，负责应用身份认证和权限校验。
- ALB：应用负载均衡。
- Agent 网关初始版本：已有基础能力，支持 A2A 协议通信。
- MCP 网关初始版本：已有基础能力，支持工具注册及工具调用。

本项目当前主要负责：

- Agent Gateway 的补强建设。
- 策略中心的设计与实现。

MCP Gateway 由同事负责实现。涉及 MCP Gateway 的改造点，需要在设计文档中明确提出、给出接口和协议要求，并知会相关负责人，但不由本项目直接完成 MCP Gateway 代码实现。

后续设计必须清晰区分：

- 哪些能力由 Agent Gateway 实现。
- 哪些能力由策略中心实现。
- 哪些能力需要 MCP Gateway 配合。
- 哪些能力复用 IDaaS、IAM、ALB 等公司已有系统。
- 哪些能力属于业务后端、Agent 或 MCP Server 的职责。

## 5. 需要保留的核心方向

虽然初版材料存在较多问题，但以下方向应被保留并深化：

### 5.1 Cookie 不直接透传给 Agent

Agent 不应直接持有用户 Cookie。用户 Cookie 代表用户在传统系统中的完整登录态，如果透传给 Agent，会导致 Agent 拥有过大权限，难以限制和追责。

正确方向是：由受信任的网关在安全边界内完成 Cookie 保管、换取和传统 API 调用支撑。Agent 只持有经过约束的任务级凭证。

### 5.2 任务级 Token

Agent 调用工具时不应使用用户 Cookie，而应使用由 Agent Gateway 生成的任务级 Token。

Token 至少应绑定：

- 用户身份。
- Agent 身份。
- Task/会话上下文。
- 策略版本或策略快照。
- 允许的受众。
- 有效期。
- 可撤销状态。
- 委托链上下文。

Token 不应只是一个包含 userId、agentId、taskId 的普通 JWT。

### 5.3 策略中心统一决策

策略中心应成为 Agent 委托授权的统一决策点，负责根据用户、Agent、工具、数据范围、操作风险、委托关系、确认状态、A2A 链路等上下文做权限决策。

策略中心不应只是委托策略 CRUD 系统，而应具备明确的 Policy Decision Point 能力。

### 5.4 MCP Gateway 作为工具调用控制点

MCP Gateway 是工具调用入口，应承担工具调用鉴权、参数注入、调用上下文传递、审计埋点和 Cookie 还原调用传统 API 的控制点职责。

Agent 不应绕过 MCP Gateway 直接调用 MCP Server 或传统 API。

### 5.5 人在环确认

对于高风险工具、高风险数据、高风险操作或超出默认授权范围的请求，应支持人在环确认。

人在环确认不是简单弹窗，而是一次安全授权决策。确认结果必须绑定具体操作和参数摘要，并且只能被使用一次或在极短时间内受限使用。

### 5.6 A2A 委托链

Agent 调用 Agent 时，必须传递可验证的委托链。委托链应支持权限收敛、深度限制、目标 Agent 限制和完整审计。

A2A 的核心不是让下游 Agent 继承上游 Agent 的全部权限，而是在用户授权、上游 Agent 能力、目标 Agent 能力和工具风险之间计算最小有效权限。

## 6. 必须重构的问题域

后续 ChatGPT 生成正式设计文档时，必须重点重构以下内容。

### 6.1 信任边界

初版材料没有清晰描述各组件之间的信任关系。

新设计必须明确：

- 用户、业务后端、Agent Gateway、策略中心、MCP Gateway、MCP Server、Agent、IDaaS、IAM、ALB 分别处于什么信任域。
- 哪些组件可以持有用户 Cookie。
- 哪些组件可以生成、验证、吊销 Token。
- 哪些组件可以查询或变更委托策略。
- 哪些调用必须使用 mTLS、应用身份认证、服务间鉴权或签名。
- 任一组件被攻破时，影响范围如何限制。

### 6.2 Token 模型

初版 Token 模型过于简单。

新设计应补充：

- Token 类型：用户会话 Token、任务级 Agent Token、A2A 子 Token、确认票据等是否需要拆分。
- Token claims：issuer、audience、subject、agentId、taskId、policySnapshotId、scope、dataScope、delegationChainId、jti、iat、exp、nonce 等。
- Token 生命周期：生成、激活、使用、续期限制、过期、撤销、异常锁定。
- Token 验证：签名验证、受众验证、用途验证、状态验证、策略版本验证、重放检测。
- Token 与 Cookie 的关系：Token 不等于 Cookie，Token 只能作为受限凭证。

### 6.3 Cookie 换取与保管

初版材料虽然提出 Cookie 不透传给 Agent，但又在多个流程中让网关缓存和换取 Cookie，边界不清晰。

新设计必须明确：

- Cookie 是否只允许 Agent Gateway 持有，还是 MCP Gateway 也可短时持有。
- Cookie 是否允许落 Redis，如允许，需要使用什么加密、TTL、访问控制和审计。
- MCP Gateway 换取 Cookie 的接口必须如何鉴权。
- Cookie 换取是否必须绑定 Token、工具、API、数据范围和调用目的。
- Cookie 泄露或 Token 吊销时如何清理缓存。
- 是否可以用更安全的后端服务令牌替代 Cookie 调用传统 API。

### 6.4 策略模型

初版策略中心设计偏 CRUD，不足以支撑安全委托。

新设计应定义统一策略模型：

- 主体：用户、Agent、系统服务。
- 客体：工具、MCP Server、传统 API、数据对象。
- 动作：read、create、update、delete、execute、approve 等。
- 环境：租户、应用、地域、时间、风险等级、调用来源。
- 委托：用户到 Agent、Agent 到 Agent、临时确认授权。
- 决策结果：allow、deny、need_confirmation、step_up_auth、fallback_deny。
- 冲突规则：deny 优先、最小权限、最严格确认规则优先。
- 策略版本：每次授权决策应能关联到策略版本或快照。

### 6.5 人在环确认模型

初版材料中人在环确认流程过于松散。

新设计应明确：

- 什么条件触发确认。
- 确认请求如何生成。
- 用户看到哪些风险说明和操作详情。
- 确认票据绑定哪些字段：toolName、operation、resourceId、参数哈希、taskId、agentId、userId、过期时间、一次性 nonce。
- 确认后由谁通知 Agent 继续执行。
- Agent 检查点挂起与恢复如何实现。
- 用户拒绝、超时、重复点击、参数变化、策略变化时如何处理。

### 6.6 A2A 委托链模型

初版材料提出 A2A 委托链，但存在简单继承权限的风险。

新设计应明确：

- A2A 是否允许默认关闭。
- A2A 目标 Agent 是否必须显式 allowlist。
- A2A 子 Token 如何生成。
- 委托链如何签名、校验、防篡改、防重放。
- 多级链路最大深度。
- 权限如何计算交集。
- 下游 Agent 是否允许进一步调用其他 Agent。
- A2A 调用如何审计和可视化。

### 6.7 Agent 注册与供应链安全

初版 Agent 注册只关注元数据管理，缺少准入安全。

新设计应补充：

- Agent 注册审批。
- Agent owner、版本、能力声明、可调用工具、A2A 能力声明。
- Agent endpoint allowlist。
- Agent 服务身份认证。
- Agent 发布、更新、禁用、下线、灰度和回滚。
- Agent 能力声明与实际调用行为的一致性校验。
- 恶意或异常 Agent 的隔离策略。

### 6.8 审计与可观测性

初版材料有审计字段，但缺少可取证能力。

新设计应补充：

- 统一 traceId / taskId / tokenId / decisionId / auditId。
- 权限决策日志。
- 工具调用日志。
- Cookie 换取日志。
- 人在环确认日志。
- A2A 委托链日志。
- 策略变更日志。
- 审计日志脱敏、签名、防篡改、留存周期和查询能力。
- 安全告警闭环。

## 7. 新四份设计文档的生成目标

后续需要由 ChatGPT 基于本文档和初版材料重新生成四份正式设计文档。

建议生成以下四份：

### 7.1 Agent 人机委托机制总体架构设计

目标：定义整体安全架构、业务流程、信任边界、威胁模型、核心概念和跨服务职责。

应包含：

- 背景与问题。
- 公司已有系统约束。
- 总体架构。
- 信任边界。
- 核心安全原则。
- 关键调用链路。
- 人在环授权链路。
- A2A 委托链路。
- 威胁模型与缓解措施。
- 服务职责边界。
- 分阶段建设路线。

### 7.2 Agent Gateway 详细设计

目标：定义 Agent Gateway 负责的能力和接口。

应包含：

- Agent 入口代理。
- 用户 Cookie 接入与会话管理。
- 任务级 Token 生成、验证、吊销。
- A2A 子 Token 生成。
- Agent 注册与查询。
- Cookie 换取接口的安全边界。
- 与 IDaaS、IAM、策略中心、MCP Gateway 的交互。
- 异常处理、审计、监控。
- 不负责的能力边界。

### 7.3 策略中心详细设计

目标：定义策略中心作为统一权限决策点的设计。

应包含：

- 策略模型。
- 委托关系模型。
- 工具/Agent 标签模型。
- 权限决策接口。
- 人在环确认策略。
- A2A 委托链解析。
- 策略版本与快照。
- 冲突检测和影响分析。
- 审计与告警。
- 管理端配置流程。

### 7.4 MCP Gateway 配合改造设计

目标：明确 MCP Gateway 需要配合实现或改造的点，供负责 MCP Gateway 的同事评审和实现。

应包含：

- 工具调用前鉴权。
- 携带 Agent Token 调用策略中心。
- 参数注入和数据范围约束。
- Cookie 换取调用 Agent Gateway 的安全约束。
- MCP Server/API 调用审计。
- 人在环确认返回协议。
- 与 Agent 检查点恢复机制的衔接。
- 需要新增或调整的接口字段。

## 8. 对 ChatGPT 的生成要求

使用 ChatGPT 重新生成四份设计文档时，应遵循以下要求：

1. 不要照搬 GLM-5 初稿中的接口、数据结构和流程。
2. 先建立信任边界、威胁模型和职责边界，再展开接口和数据结构。
3. 所有安全相关流程必须默认拒绝，只有明确授权才允许。
4. 所有 Token、Cookie、确认票据、委托链都必须说明生成、验证、过期、撤销和审计机制。
5. 所有 A2A 权限都必须按最小权限和权限交集处理，不能简单继承。
6. 所有人在环确认都必须绑定具体操作和参数摘要，避免确认被重放或挪用。
7. 所有 MCP Gateway 改造点必须单独列出，便于知会同事。
8. 文档应面向后续代码开发，避免空泛描述。
9. 每个服务的“不负责什么”也要写清楚。
10. P0/P1 优先级必须按安全闭环排序，而不是按功能表面复杂度排序。

## 9. 非目标

本项目当前阶段不追求以下目标：

- 不重建 IDaaS。
- 不重建 IAM。
- 不重建 ALB。
- 不重写 MCP Gateway 全量能力。
- 不设计通用企业权限平台。
- 不把 Agent 委托授权扩展成所有系统统一鉴权方案。
- 不为了性能提前引入高风险优化，例如 Token 预生成、Cookie 批量预取、长期 Cookie 缓存。

当前阶段应优先完成 Agent 委托授权安全闭环，再考虑性能优化、可视化、智能推荐等增强能力。

## 10. 判断标准

新的设计文档如果满足以下条件，才可进入开发拆解：

- 清楚说明每个服务的职责和边界。
- 清楚说明用户、Agent、MCP Gateway、Agent Gateway、策略中心之间的信任关系。
- 清楚说明 Token、Cookie、策略、确认票据、委托链的生命周期。
- 清楚说明无权限、权限不足、策略服务异常、Token 过期、用户拒绝确认、Agent 异常等失败路径。
- 清楚说明 MCP Gateway 需要配合改造的点。
- 安全策略能防止 Cookie 透传、Token 重放、A2A 权限放大、确认挪用、策略绕过和审计缺失。
- 每一项核心能力都能进一步拆成接口、数据结构、测试用例和开发任务。

只有满足以上标准，新的四份设计文档才可以作为后续代码开发依据。
