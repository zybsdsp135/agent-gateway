# 公司补充背景

## 1. 项目定位

本项目是公司级 Agent 安全委托授权项目，主要服务于公司内部的 Agent 项目，例如财经 Eureka X 等云侧 Agent 场景。

项目核心目标是解决云侧 Agent 在调用工具、访问传统后端能力、进行 A2A 协作时的安全委托问题。重点关注用户身份、Agent 身份、任务级授权、工具调用鉴权、Cookie 换取、人在环确认、A2A 委托链传递等能力。

## 2. 内外网协作背景

公司内网环境不能访问 ChatGPT，只能使用公司内已有 AI 开发工具。目前公司内 AI 工具使用的模型主要是 GLM-5，模型能力相对有限，适合做初步头脑风暴和辅助生成，但不足以支撑完整、严肃、可落地的 Agent 安全架构设计。

因此，本项目采用内网与外网协作开发模式：

- 公司内网负责接触真实业务背景、内部系统、公司已有网关与鉴权能力、架构图和约束信息。
- 外网借助 ChatGPT 的更强推理与设计能力，协助完成 Agent 委托授权安全方案的梳理、补强、重构和开发落地。
- 外网协作材料应避免包含敏感生产数据、密钥、真实用户隐私、内部不可外传接口细节等内容。

该协作模式的目的不是让外网直接替代公司内实现，而是借助更强模型能力，把初版想法整理成可以在公司内落地开发的安全设计、接口边界和实现任务。

## 3. 公司已有基础设施

公司内部已经存在以下基础设施和服务：

- SSO 单点登录服务：IDaaS 服务，负责用户登录与用户身份认证。
- 鉴权服务：IAM 服务，负责应用身份认证、权限校验等鉴权能力。
- 应用负载均衡：ALB。
- Agent 网关初始版本：已有初始能力，支持 A2A 协议通信。
- MCP 网关初始版本：已有初始能力，支持工具注册及工具调用。

这些已有能力是本项目设计的基础约束。后续设计不应重复建设 IDaaS、IAM、ALB、MCP 网关已有通用能力，而应明确复用方式、调用边界和需要补充改造的点。

## 4. 当前建设目标与责任边界

当前主要建设目标是 Agent 网关和策略中心。

其中：

- Agent 网关是当前需要重点负责建设的服务之一，主要涉及 Token 生成与管理、Cookie 换取、Agent 注册管理、Agent 入口代理、A2A 调用链路中的身份与委托信息传递等。
- 策略中心是当前需要重点负责建设的服务之一，主要涉及工具/Agent 标签管理、委托关系管理、权限决策引擎、策略审计、人在环确认相关策略等。
- MCP 网关由同事负责实现。涉及 MCP 网关需要配合改动的点，需要在设计中明确说明并知会相关同事，但不需要本人直接实现 MCP 网关代码。

后续设计文档需要清晰区分：

- 本项目必须实现的 Agent 网关能力。
- 本项目必须实现的策略中心能力。
- MCP 网关需要配合改造的接口、协议、鉴权调用或调用链上下文传递能力。
- IDaaS、IAM、ALB 等公司已有系统需要复用或对接的能力。

## 5. 初版设计材料说明

当前仓库中的四份设计文档是公司领导基于公司内 GLM-5 模型头脑风暴生成的初版材料：

- `design_doc/Agent人机委托机制架构设计文档.md`
- `design_doc/Agent Gateway详细设计文档.md`
- `design_doc/MCP Gateway详细设计文档.md`
- `design_doc/策略中心详细设计文档.md`

这四份文档只能作为问题域、功能清单和初始思路参考，不应视为最终开发版本。

主要原因：

- 生成模型能力有限，很多设计只停留在概念层面。
- 安全边界、信任模型、Token 模型、Cookie 换取边界、A2A 委托链、人在环确认、策略决策模型等关键细节仍不完整。
- 部分章节存在 AI 文档常见问题，例如泛化描述较多、关键安全机制缺少可执行约束、职责边界不够清晰、验收标准偏愿望清单。
- 后续进入代码开发前，需要对这些方向和细节进行重构、补充和纠偏。

因此，后续应以重新梳理后的安全架构、接口契约、威胁模型和实现计划为准。

## 6. 方案图说明

当前有两类方案图材料：

- 两张由本人在公司内网绘制的顶层设计图：
  - 方案全景图。
  - 人在环授权委托调用链路图。
- 仓库 `img_doc` 目录下已有一张整体方案图：
  - `img_doc/整体方案图.png`

这些图主要用于表达顶层方案、关键服务关系和主要调用链路，不涉及完整实现细节。

方案全景图表达的核心链路包括：

- 人类用户访问智能体。
- 业务后端通过 IDaaS 完成用户登录，通过 IAM 完成应用身份凭证与校验。
- 业务后端将 Cookie 代理转发到 Agent 网关。
- Agent 网关进行用户令牌校验、应用身份校验、Token 生成与管理、Cookie 换取、Agent 注册管理。
- Agent 通过 Exchange Token 获取包含用户身份、Agent 身份和策略 ID 的任务级凭证。
- Agent 调用 MCP 网关时携带 Token。
- MCP 网关向策略中心进行权限决策，并在需要时向 Agent 网关换取 Cookie。
- MCP 网关使用 Cookie 调用 MCP Server/API。

人在环授权委托调用链路图表达的核心链路包括：

- Agent 首次调用 MCP 网关时，如果策略权限不足，MCP 网关返回 403 无权限。
- Agent 将授权请求返回给 Web 后端，再由 Web 后端触达人类用户授权确认。
- 用户确认授权后，Web 后端请求策略中心新增策略。
- Agent 支持检查点挂起与恢复。
- 策略新增后，Agent 重新发起 MCP 调用。
- MCP 网关再次请求策略中心决策，策略通过后调用 MCP Server/API。

## 7. 后续设计原则

后续设计需要优先解决安全架构问题，而不是直接沿用初版文档中的接口和流程。

建议遵循以下原则：

- 默认拒绝：任何 Agent、工具、A2A、Cookie 换取、策略查询在没有明确授权时默认拒绝。
- 最小权限：Token、委托策略、A2A 继承、工具调用和数据范围都应按最小权限设计。
- 明确信任边界：区分用户、业务后端、Agent 网关、策略中心、MCP 网关、MCP Server、Agent、IDaaS、IAM、ALB 的信任边界。
- Token 任务级与用途级绑定：Token 不仅绑定 Task，还应绑定 Agent、工具用途、受众、过期时间、调用上下文和可撤销状态。
- Cookie 不外泄：Cookie 不应透传给 Agent。若 MCP 网关需要使用 Cookie 调用传统 API，必须明确换取、缓存、加密、审计和吊销边界。
- 人在环确认不可被重放或篡改：确认结果应绑定具体工具、参数摘要、数据对象、风险说明、Task、Token 和一次性确认票据。
- A2A 权限不得简单继承：A2A 调用应按委托链权限交集、目标 Agent 限制、工具限制、数据范围限制和调用深度限制进行决策。
- 审计可追溯：关键操作需要具备完整审计链路，能够追踪用户、Agent、Task、Token、工具、策略版本、决策结果和调用结果。
- MCP 网关改造点要单独列出：涉及同事负责的 MCP 网关改造，需要在设计中明确接口、协议、调用上下文和验收标准。

## 8. 当前协作目标

当前阶段的主要目标是：

1. 基于公司真实背景和已有初版材料，重新梳理 Agent 委托授权安全架构。
2. 明确 Agent 网关、策略中心、MCP 网关、IDaaS、IAM、业务后端、Agent、MCP Server 之间的职责边界。
3. 补全威胁模型、信任边界、Token 模型、委托策略模型、人在环确认模型和审计模型。
4. 将初版设计材料重构为可评审、可拆解、可开发、可验收的正式设计文档。
5. 在设计明确后，进入 Agent 网关和策略中心的代码开发。
